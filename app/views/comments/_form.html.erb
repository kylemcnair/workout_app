

<%= form_for([@workout, @workout.comments.build]) do |f| %>

  <div class="field">
    <%= f.label :description %>
    <%= f.text_area :description %>
  </div>

  <div class="field">
    <%= f.label :creator %>
    <%= f.text_field :creator %>
  </div>

  <div>
    <%= f.submit %>
  </div>
<% end %>
